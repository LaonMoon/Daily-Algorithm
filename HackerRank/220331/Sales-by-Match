def sockMerchant(n, ar):
    # Write your code here
    count = 0
    num_set = set(ar)
    num_list = list(num_set)
    for i in range(len(num_list)):
        count += ar.count(num_list[i]) // 2
    return count
