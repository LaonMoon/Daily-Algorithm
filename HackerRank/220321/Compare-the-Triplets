def compareTriplets(a, b):
    alice_point = 0
    bob_point = 0
    for i in range(3):
        if a[i]>b[i]:
            alice_point += 1
        if a[i]<b[i]:
            bob_point += 1
    return [alice_point, bob_point]
